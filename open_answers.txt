Task 0a:
Answer by running readelf -h abc
1. Where is the entry point specified, and what is its value?
Answer: 0x80483b0

How many sections are there in "abc"?
Answer: 29

---------

Task 2 Questions:
1. Which "function" should precede main() in execution?
The function that must precede main() is _start.
an executable doesn't just start magically at main(). 
The operating system transfers control to the _start routine, which sets up the program's environment, initializes standard standard input/output, and pushes command-line arguments onto the stack before finally calling main(). 
If an ELF file's entry point is mistakenly set directly to main, the program will crash because the environment wasn't properly initialized.

2. What is the virtual address to which this function is loaded?
08048350


Task 2 fix:
Step 1: Setup
1. Run your ./hexeditplus program.  
2. Press F to set the file name to deep_thought.
3. Press U to set the unit size to 4 (since memory addresses are 4 bytes long in a 32-bit ELF).  

Step 2: Read the existing (broken) entry point
1. Press L (Load Into Memory) to load the ELF header into your buffer.  
* Prompt: <location> <length>
* Input: 18 1 (Offset 0x18 is the exact byte location of the Entry Point in a 32-bit ELF header, and we want to read 1 unit of 4 bytes).
2. Press M (Memory Display) to see what is currently there.  
* Prompt: <addr> <u units>
* Input: 0 1 (Read 1 unit from the start of your memory buffer). You will see the broken entry point address.

Step 3: Patch the memory buffer
1. Press Y (Memory Modify) to overwrite the broken address in your buffer with the correct one.  
* Prompt: <location> <val>
* Input: 0 <address_of_start> (Replace <address_of_start> with the hex address you found using readelf -s in Part 1, without the 0x prefix).

Step 4: Save the patch to the file
1. Press S (Save Into File) to write your corrected memory buffer back into the deep_thought binary.  
* Prompt: <source-address> <target-location> <length>
* Input: 0 18 1 (Take 1 unit from source-address 0 in your buffer, and write it to the target-location 0x18 in the actual file).

Step 5: Verify and Run
1. Quit hexeditplus (Option Q).  
2. Run readelf -h deep_thought to verify that the "Entry point address" matches your _start address.  
3. Run ./deep_thought. It should now execute perfectly!

---------

Task 3:
address: 0804841d (use readelf -s)
size: 23
section: 13

(then do readelf -S offensive)
addr: 08048320
offset: 000320

https://www.calculator.net/hex-calculator.html?number1=000320&c2op=%2B&number2=FD&calctype=op&x=Calculate

Offset_Within_Section = (main_Value_Addr) - (Section_Addr) = 0804841d - 08048320 = FD
main_File_Offset = (Section_Off) + Offset_Within_Section = 000320 + FD = 41D


1. Run ./hexeditplus.

2. F (Set File Name): Set it to offensive.

3. U (Set Unit Size): Set it to 1 (we want to see individual instruction bytes).

4. L (Load Into Memory): 
* Location: Enter the main_File_Offset you calculated in Step 3 (in hex). (main_File_Offset=41D)
* Length: Enter the Size you found in Step 1 (in decimal). (size=23)

5. M (Memory Display):
* Address: 0 (read from the start of your memory buffer).
* Length: Enter the Size again. (size=23)

You are now looking at the raw machine code (OpCodes) of the main function! The last byte printed should be c3, which is the x86 instruction for ret (return).


Part 4:

1. Find the buggy function in ntsc:

* Run readelf -s ntsc | grep count_digits. Note its Size and its virtual Value address. [size=93, address 0804847d]

* Run readelf -S ntsc to find the .text section's virtual address and file offset. [address = 08048380, offset = 000380]

Offset_Within_Section = (count_digits_Value_Addr) - (Section_Addr) = 0804847d - 08048380 = FD
count_digits_offest = (Section_Off) + Offset_Within_Section = 000380 + FD = 47D

* Calculate the exact file offset for ntsc's count_digits. (Let's call this NTSC_OFFSET = 10090B7D).

2. Find your new function in task4:

* Run readelf -s task4 | grep count_digits. Note its Size and its virtual Value address. (Ensure this size is <= the size from step 1!)
size = 80, address = 0000118d

* Run readelf -S task4 to find the .text section offset.
offest = 001060, address = 00001060

* Calculate the exact file offset for task4's count_digits. (Let's call this TASK4_OFFSET).

Offset_Within_Section = (count_digits_Value_Addr) - (Section_Addr) = 0000118d - 00001060 = 12D
count_digits_offest = (Section_Off) + Offset_Within_Section = 118D


3. Run ./hexeditplus

4. U (Set Unit Size): Set to 1 (we are moving exact bytes).

5. F (Set File Name): Enter task4

6. L (Load Into Memory):

* Prompt: <location> <length>

* Input: Enter your TASK4_OFFSET (in hex) and the Size of your new function (in decimal). Your global mem_buf now holds the correct machine code!

7. F (Set File Name): Enter ntsc (We are switching targets).

8. S (Save Into File):

*Prompt: <source-address> <target-location> <length>

* Input: 0 <NTSC_OFFSET> <Size>
(This takes the bytes starting at index 0 of your mem_buf and writes them into ntsc exactly over the buggy function, for the length of your new function).